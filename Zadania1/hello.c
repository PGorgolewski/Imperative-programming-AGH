#include<stdio.h>
#include<stdlib.h>

void qsort ( void * tab, size_t num, size_t size, int ( * comparator ) ( const void *, const void * ) );

int main(){
    int m;
    scanf("%d", &m);
    printf("hello world");
    return 0;
}